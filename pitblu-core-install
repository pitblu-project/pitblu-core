#!/usr/bin/env python3
"""Run the guided installer directly from a trusted source checkout."""

import sys
from pathlib import Path

SOURCE_ROOT = Path(__file__).resolve().parent
sys.path.insert(0, str(SOURCE_ROOT / "src"))

from pitblu_core.install_cli import main  # noqa: E402

raise SystemExit(main(source_root=SOURCE_ROOT))
