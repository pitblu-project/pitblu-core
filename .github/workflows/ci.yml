name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml
      - run: python -m pip install --upgrade pip setuptools==83.0.0
      - run: python -m pip install -e ".[dev]"
      - run: python -m ruff check .
      - run: python -m ruff format --check .
      - run: python -m mypy src tests
      - run: bash -n deploy/manage.sh
      - run: python -m pytest -vv -o faulthandler_timeout=30
      - name: Build and install standalone wheel
        run: |
          python -m pip wheel --no-deps . --wheel-dir dist
          python -m venv "$RUNNER_TEMP/pitblu-wheel-venv"
          "$RUNNER_TEMP/pitblu-wheel-venv/bin/python" -m pip install dist/*.whl
          cd "$RUNNER_TEMP"
          "$RUNNER_TEMP/pitblu-wheel-venv/bin/python" -c 'from importlib.metadata import version; import pitblu_core; assert version("pitblu-core") == pitblu_core.__version__'
          "$RUNNER_TEMP/pitblu-wheel-venv/bin/pitblu-state" --help
          "$RUNNER_TEMP/pitblu-wheel-venv/bin/pitblu-ble-proof" --help
          "$RUNNER_TEMP/pitblu-wheel-venv/bin/pitblu-v202-check" --help
      - name: Dependency inventory
        run: |
          python -m pip install hatchling==1.32.0
          python scripts/dependency_inventory.py > dependency-inventory.json
      - name: Known dependency vulnerabilities
        run: |
          python -m pip install pip-audit==2.10.1
          python -m pip_audit --local --skip-editable --format json --output dependency-audit.json
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: dependency-audit-python-${{ matrix.python-version }}
          path: |
            dependency-inventory.json
            dependency-audit.json
